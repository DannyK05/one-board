mutation CreateUser($email: EmailAddress!, $password: String!) {
  createUser(data: { email: $email, password: $password }) {
    accessToken
    refreshToken
    user {
      id
      email
      avatar
    }
  }
}
